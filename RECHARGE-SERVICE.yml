server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://mysql-db:3306/omnicharge_recharge
    username: root
    password: user
  jpa:
    hibernate:
      ddl-auto: update
  rabbitmq:
    host: rabbitmq-broker

eureka:
  client:
    service-url:
      defaultZone: http://service-registry:8761/eureka/

resilience4j:
  circuitbreaker:
    instances:
      operatorService:
        registerHealthIndicator: true
        slidingWindowSize: 5
        failureRateThreshold: 50
        waitDurationInOpenState: 10s
        permittedNumberOfCallsInHalfOpenState: 3
