server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://mysql-db:3306/omnicharge_user
    username: root
    password: user
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://service-registry:8761/eureka/
