server:
  port: 8085

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/omnicharge_notification
    username: root
    password: user
  jpa:
    hibernate:
      ddl-auto: update
  rabbitmq:
    host: localhost

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
