server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://mysql-db:3306/omnicharge_operator
    username: root
    password: user
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://service-registry:8761/eureka/
